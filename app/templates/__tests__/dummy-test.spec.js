describe("WHEN this dummy test runs", () => {
  it("SHOULD pass", () => {
    // Arrange
    const expected = true;

    // Act
    const actual = true;

    // Assert
    expect(expected).toBe(actual);
  });
});
